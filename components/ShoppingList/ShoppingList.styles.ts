import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    subtitle: {
        textAlign: 'center',
        fontSize: 24,
        color: '#fff',
        marginBottom: 40,
      },
})

export default styles